{
  "name": "@clinchec/fhir-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "build": "echo \"@clinchec/fhir-client ships TypeScript source, consumed via Next transpilePackages\" && exit 0",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}