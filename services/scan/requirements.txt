# --- Web framework ---------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2

# --- NLP -------------------------------------------------------------------
spacy==3.8.7
# Clinical NER weights are pulled at image build time:
#   python -m spacy download en_core_web_sm
# For the fine-tuned clinical model, set SPACY_CLINICAL_MODEL and install e.g.
#   pip install scispacy && pip install <en_core_sci_md wheel url>

# --- LLM justification drafting --------------------------------------------
openai==1.51.0

# --- Persistence -----------------------------------------------------------
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
redis==5.0.8

# --- Observability ---------------------------------------------------------
structlog==24.4.0
